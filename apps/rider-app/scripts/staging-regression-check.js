const fs = require("node:fs");
const path = require("node:path");

const appRoot = path.resolve(__dirname, "..");
const read = (relativePath) => fs.readFileSync(path.join(appRoot, relativePath), "utf8");
const readJson = (relativePath) => JSON.parse(read(relativePath));

const failures = [];
const expect = (condition, message) => {
  if (!condition) failures.push(message);
};

const packageJson = readJson("package.json");
const appJson = readJson("app.json");
const easJson = readJson("eas.json");
const appConfig = read("app.config.ts");
const rootLayout = read("app/_layout.tsx");
const apiClient = read("src/api/client.ts");
const authContext = read("src/contexts/auth-context.tsx");
const loginScreen = read("app/auth/login.tsx");
const applicationScreen = read("app/auth/apply.tsx");
const forgotPasswordScreen = read("app/auth/forgot-password.tsx");
const resetPasswordScreen = read("app/auth/reset-password.tsx");
const applicantOnboardingApi = read("src/api/applicant-onboarding.api.ts");
const captainAccessApi = read("src/api/captain-access.api.ts");
const captainApplicationIntent = read("src/lib/captain-application-intent.ts");
const dashboard = read("app/tabs/dashboard.tsx");
const jobsIndex = read("app/jobs/index.tsx");
const jobDetail = read("app/jobs/[id].tsx");
const earnings = read("app/earnings.tsx");
const profile = read("app/profile.tsx");
const riderNav = read("src/components/rider-navigation.tsx");
const taxiReadiness = read("app/taxi-readiness.tsx");
const taxiApi = read("src/api/taxi.api.ts");
const deliveryCaptainApplicationsApi = read("src/api/delivery-captain-applications.api.ts");
const captainCatalogApiSource = read("src/api/captain-catalog.api.ts");
const ui = read("src/components/ui.tsx");
const captainModes = read("src/lib/captain-modes.ts");

const stagingProfile = easJson.build?.["rider-staging"];

expect(!JSON.stringify(packageJson).includes("eas-cli"), "Rider app must not depend on eas-cli.");
expect(stagingProfile?.distribution === "internal", "rider-staging must use internal distribution.");
expect(stagingProfile?.android?.buildType === "apk", "rider-staging must produce an Android APK.");
expect(
  stagingProfile?.env?.EXPO_PUBLIC_API_BASE_URL === "https://karigo-8htn.onrender.com/api/v1",
  "rider-staging must point at the Render staging API."
);
expect(stagingProfile?.env?.APP_VARIANT === "staging", "rider-staging must set APP_VARIANT=staging.");

expect(appConfig.includes("com.karigo.rider.staging"), "Staging Android package/iOS bundle ID must be configured.");
expect(appConfig.includes("KariGO Captain Staging"), "Staging app name must be configured.");
expect(appConfig.includes("EXPO_PUBLIC_API_BASE_URL"), "App config must read the public staging API URL.");
expect(appConfig.includes("344a78dc-69d9-4daa-9616-f100b67f0910"), "Rider EAS project ID must be linked in app config.");
expect(!appConfig.includes("344a78dc-69d3-4aa-9616-fb1b067f0910"), "Rider app config must not contain the old invalid EAS project ID.");
expect(appConfig.includes("https://u.expo.dev/${riderEasProjectId}"), "Rider EAS Update URL must be configured.");
expect(appConfig.includes('policy: "appVersion"'), "Rider runtimeVersion must use the appVersion policy.");
expect(packageJson.dependencies?.["expo-updates"] === "~0.28.18", "Rider app must use the Expo SDK 53-compatible expo-updates package.");
expect(packageJson.dependencies?.["expo-location"] === "~18.1.6", "Captain app must use the Expo SDK 53-compatible expo-location package.");
expect(packageJson.dependencies?.["expo-local-authentication"] === "~16.0.5", "Captain app must use the Expo SDK 53-compatible expo-local-authentication package.");
expect(packageJson.dependencies?.["expo-build-properties"] === "~0.14.8", "Captain app must use Expo build-properties for Android API 36 readiness.");
expect(appConfig.includes("expo-location") && appConfig.includes("locationWhenInUsePermission"), "Captain app config must include safe foreground location permission copy.");
expect(JSON.stringify(appJson.expo.plugins).includes("expo-build-properties"), "Captain app base config must include build-properties.");
expect(JSON.stringify(appJson.expo.plugins).includes('"targetSdkVersion":36'), "Captain app base config must target Android API 36.");
expect(appConfig.includes("expo-build-properties"), "Captain app config must register Android build-properties.");
expect(appConfig.includes("compileSdkVersion: 36"), "Captain app must compile against Android API 36.");
expect(appConfig.includes("targetSdkVersion: 36"), "Captain app must target Android API 36.");
expect(appConfig.includes('buildToolsVersion: "36.0.0"'), "Captain app must use Android build tools 36.0.0.");
expect(appConfig.includes('version: "0.1.1"'), "Captain app version must be bumped to 0.1.1 for the secure onboarding AAB.");
expect(appConfig.includes("versionCode: isStaging ? 1 : 10"), "Captain production versionCode must be bumped to 10 for the next Play-ready AAB.");
expect(appConfig.includes('"@react-native-community/datetimepicker"'), "Captain app config must register the native date picker plugin.");
expect(packageJson.dependencies?.["expo-image-picker"] === "~16.1.4", "Captain app must use Expo SDK 53-compatible image picker for secure uploads.");
expect(packageJson.dependencies?.["expo-document-picker"] === "~13.1.6", "Captain app must use Expo SDK 53-compatible document picker for secure uploads.");
expect(packageJson.dependencies?.["@react-native-community/datetimepicker"] === "8.4.1", "Captain app must use the SDK-compatible native date picker.");

expect(apiClient.includes("karigo_rider_access_token"), "Rider token storage key must be rider-specific.");
expect(apiClient.includes("karigo_rider_refresh_token"), "Rider refresh token storage key must be rider-specific.");
expect(apiClient.includes("refreshAuth"), "Captain app must refresh sessions when access tokens expire.");
expect(apiClient.includes("createApiClient"), "Rider app must use the shared API client.");
expect(authContext.includes("canUseCaptainApp"), "Captain app must use an explicit account eligibility helper.");
expect(authContext.includes('user.role === "RIDER" || user.role === "CUSTOMER"'), "Captain app must allow Customer accounts for onboarding/status and Rider accounts for approved operations.");
expect(authContext.includes("cannot use the Captain app"), "Non-customer/non-captain rejection copy must use Captain branding.");
expect(!authContext.includes("error.status === 401 || error.status === 403"), "Captain auth context must not clear saved sessions for ordinary forbidden responses.");
expect(authContext.includes("authApi.logout") && authContext.includes("refreshTokenStore"), "Captain logout must clear persisted refresh sessions safely.");
expect(authContext.includes("refreshWithBiometrics") && authContext.includes("authApi.refresh"), "Captain biometric sign-in must refresh a saved backend session.");
expect(authContext.includes("setBiometricSignIn"), "Captain auth context must expose biometric setup controls.");
expect(ui.includes("PasswordField"), "Rider shared UI must include a password visibility field.");
expect(ui.includes("visible ? \"Hide\" : \"Show\""), "Rider password field must expose show/hide toggle copy.");
expect(loginScreen.includes("PasswordField") && loginScreen.includes("passwordVisible"), "Rider login password field must support visibility toggling.");
expect(loginScreen.includes("Sign in with biometrics"), "Captain login must offer biometric sign-in when enabled.");
expect(loginScreen.includes("This account can continue Captain onboarding. Sign in to proceed."), "Captain login must explain existing account onboarding.");
expect(loginScreen.includes("Forgot password?") && loginScreen.includes("/auth/forgot-password"), "Captain login must link to password reset.");
expect(loginScreen.includes("Apply to become a Captain"), "Captain login must link to in-app applicant signup.");
expect(forgotPasswordScreen.includes("authApi.requestPasswordReset") && resetPasswordScreen.includes("authApi.confirmPasswordReset"), "Captain app must support forgot-password and reset-password flows.");
expect(applicationScreen.includes("Submit Captain application"), "Captain app must include an in-app application submission flow.");
expect(applicationScreen.includes("Residential State/Territory") && applicationScreen.includes("Residential City"), "Captain application must guide applicants through dependent residential state/city selection.");
expect(applicationScreen.includes("Preferred operating areas") && applicationScreen.includes("Primary operating area"), "Captain application must collect multi-state preferred operating areas and a primary area.");
expect(applicationScreen.includes("Create account and send OTP"), "Captain application must start with account creation and OTP for new applicants.");
expect(applicationScreen.includes("This phone number already has a KariGO account. Sign in with your existing KariGO password to continue your Captain application."), "Captain application must guide existing Customer accounts to sign in instead of duplicating accounts.");
expect(applicationScreen.includes("saveCaptainApplicationIntent") && captainApplicationIntent.includes("karigo_captain_application_intent"), "Captain app must preserve existing-account application intent across sign-in.");
expect(applicationScreen.includes("You are signed in with your KariGO account. Complete your Captain application to start onboarding."), "Captain application must support signed-in Customer onboarding.");
expect(applicationScreen.includes("Verify phone"), "Captain application must verify phone OTP before details.");
expect(applicationScreen.includes("Create password"), "Captain application must require password creation before details.");
expect(applicationScreen.includes("Ride Captain") && applicationScreen.includes("rideCaptainReviewInterest"), "Captain application must keep Ride Captain review gated.");
expect(applicationScreen.includes("Delivery assignments are the approved active mode") || applicationScreen.includes("Delivery jobs are the active launch workflow"), "Captain application must use live-ready Delivery Captain copy.");
expect(applicationScreen.includes("captainCatalogApi.vehicleCatalog") && captainCatalogApiSource.includes("platform/vehicle-catalog"), "Captain application must load the guided vehicle catalog.");
expect(applicationScreen.includes("captainCatalogApi.serviceAreas") && captainCatalogApiSource.includes("platform/captain-service-areas"), "Captain application must load the active Captain service-area catalog.");
expect(applicationScreen.includes("DateTimePicker") && applicationScreen.includes("Licence expiry date"), "Captain application must use native date selection for licence expiry.");
expect(applicationScreen.includes("Vehicle make") && applicationScreen.includes("Vehicle model") && applicationScreen.includes("Vehicle year") && applicationScreen.includes("Vehicle colour"), "Captain application must use guided vehicle selectors.");
expect(applicationScreen.includes("captainDocumentsApi.upload") && applicationScreen.includes("Choose gallery") && applicationScreen.includes("Take photo") && applicationScreen.includes("Choose file"), "Captain application must upload evidence from gallery, camera or file picker.");
expect(applicationScreen.includes("Profile photo") && applicationScreen.includes("Driver licence image") && applicationScreen.includes("Vehicle exterior photo") && applicationScreen.includes("Vehicle interior photo") && applicationScreen.includes("Vehicle licence / particulars"), "Captain application must require secure core document uploads.");
expect(applicationScreen.includes("documentIds(uploads)") && applicationScreen.includes("documentIds: ids"), "Captain application must submit secure upload document IDs instead of provider URLs.");
expect(applicationScreen.includes("taxiApi.submitDriverApplication"), "Captain application must submit Ride Captain readiness applications when selected.");
expect(deliveryCaptainApplicationsApi.includes("delivery-captain-applications"), "Captain application must call the Delivery Captain endpoints.");
expect(deliveryCaptainApplicationsApi.includes("submitForCurrentUser") && deliveryCaptainApplicationsApi.includes("statusForCurrentUser"), "Captain application must support authenticated Delivery Captain linking/status.");
expect(deliveryCaptainApplicationsApi.includes("documents?"), "Captain application API type must include safe document metadata.");
expect(deliveryCaptainApplicationsApi.includes("driverLicenceNumber"), "Captain application API type must include licence number metadata.");
expect(applicantOnboardingApi.includes("auth/captain-onboarding"), "Captain application must call account-first onboarding endpoints.");
expect(taxiApi.includes("taxi/driver-applications"), "Captain Ride application must call the Ride application endpoints.");
expect(taxiApi.includes("submitDriverApplicationForCurrentUser") && taxiApi.includes("currentUserApplicationStatus"), "Captain Ride readiness must support authenticated account linking/status.");

expect(rootLayout.includes("headerTitle: \"\""), "Native route titles must be hidden.");
expect(rootLayout.includes("headerTintColor: brand.colors.charcoal"), "Back arrow should use KariGO charcoal styling.");
expect(rootLayout.includes("jobs/[id]"), "Job detail route must keep a configured header/back area.");
expect(rootLayout.includes("taxi-readiness"), "Ride readiness route must be configured with back-only navigation.");
expect(rootLayout.includes("auth/apply"), "Captain application route must be registered.");
expect(rootLayout.includes("auth/forgot-password") && rootLayout.includes("auth/reset-password"), "Captain password reset routes must be registered.");
expect(rootLayout.includes("captain-access"), "Captain access bootstrap route must be registered.");
expect(rootLayout.includes("CaptainBottomNav"), "Root layout must mount the Captain bottom navigation.");
expect(!rootLayout.includes("headerTintColor: brand.colors.primary"), "Rider headers should not use the older red default title styling.");

expect(ui.includes("paddingTop: 28"), "Shared Rider screen spacing should use compact top padding.");
expect(ui.includes("paddingBottom: 112"), "Shared Rider screens must leave room for bottom navigation.");
expect(ui.includes("RefreshControl"), "Rider screens must support pull-to-refresh where used.");
expect(ui.includes("heroTitle"), "Shared Rider UI must include stronger hero titles.");
expect(riderNav.includes("Home") && riderNav.includes("Deliveries") && riderNav.includes("Earnings") && riderNav.includes("Profile"), "Captain bottom nav must expose Home, Deliveries, Earnings and Profile tabs.");
expect(riderNav.includes("@expo/vector-icons") && riderNav.includes("Feather"), "Captain bottom nav must use proper icons.");
expect(riderNav.includes("pathname.startsWith(\"/auth\")"), "Rider bottom nav must hide on auth screens.");
expect(riderNav.includes("pathname.startsWith(\"/captain-access\")"), "Rider bottom nav must hide while access is being resolved.");
expect(riderNav.includes("pathname.startsWith(\"/taxi-readiness\")"), "Captain bottom nav must hide on Ride readiness flow.");
expect(captainModes.includes("DELIVERY_CAPTAIN") && captainModes.includes("DRIVER_CAPTAIN"), "Captain mode helper must define delivery and ride modes.");
expect(captainModes.includes("Ride Captain"), "Captain mode helper must use Ride Captain display copy.");
expect(captainModes.includes("READINESS_ONLY"), "Ride Captain mode must still type readiness-only state for disabled builds.");
expect(captainModes.includes("isTaxiStagingEnabled"), "Ride Captain mode must stay gated by ride staging flags.");
expect(captainModes.includes("EXPO_PUBLIC_RIDES_SERVICE_ENABLED"), "Ride Captain mode helper must use the new Rides service flag.");
expect(captainModes.includes("EXPO_PUBLIC_RIDES_CONTROLLED_PILOT_ENABLED"), "Ride Captain mode helper must use the new controlled-pilot flag.");
expect(captainModes.includes("EXPO_PUBLIC_TAXI_SERVICE_ENABLED") && captainModes.includes("EXPO_PUBLIC_TAXI_STAGING_DISPATCH_ENABLED"), "Ride Captain mode helper must preserve legacy Taxi flag aliases.");
expect(captainModes.includes("Operations active") && captainModes.includes("Review only"), "Ride Captain mode helper must use live-ready public copy.");
expect(appConfig.includes("karigo-icon.png") && appConfig.includes("karigo-adaptive-icon.png"), "Captain App config must use square icon and adaptive icon assets.");
expect(captainAccessApi.includes("captain/access") && captainAccessApi.includes("operationalModes"), "Captain app must use the backend access resolver before dashboard operations.");
expect(dashboard.includes("karigo-logo.png"), "Dashboard must use compact KariGO branding.");
expect(dashboard.includes("Availability"), "Dashboard must show one clear availability section.");
expect(dashboard.indexOf("captainAccessApi.resolve") < dashboard.indexOf("riderApi.profile"), "Dashboard must resolve Captain access before calling rider-only operational APIs.");
expect(dashboard.includes("!access.operationalModes.includes(\"DELIVERY_CAPTAIN\")"), "Dashboard must skip delivery profile/jobs calls for unapproved applicants.");
expect(dashboard.includes("Loading Captain status"), "Dashboard fallback copy must use Captain branding.");
expect(dashboard.includes("Only active approved Delivery Captains"), "Dashboard approval copy must use Captain branding.");
expect(dashboard.includes("Manage your delivery assignments and availability."), "Dashboard must include polished Captain-facing intro copy.");
expect(dashboard.includes("Captain onboarding") && dashboard.includes("View application status"), "Dashboard must show pending applicant status instead of dispatch tools.");
expect(dashboard.includes("Captain operations will be available after KariGO approves your application."), "Dashboard must show blocked operations copy for pending applicants.");
expect(dashboard.includes("View activation status"), "Dashboard must show activation status action for approved inactive applications.");
expect(!dashboard.includes("Captain modes"), "Dashboard must not repeat Captain modes from Profile.");
expect(!dashboard.includes("Ride review"), "Dashboard must not repeat Ride Captain readiness from Profile.");
expect(dashboard.includes("Online") && dashboard.includes("Unavailable") && dashboard.includes("On delivery") && dashboard.includes("Offline"), "Dashboard must expose captain-friendly online/offline states.");
expect(dashboard.includes("Go online") && dashboard.includes("Go offline"), "Dashboard must expose online/offline availability.");
expect(dashboard.includes("requestCaptainForegroundLocation"), "Dashboard must request device GPS before going online.");
expect(dashboard.includes("riderApi.updateLocation"), "Dashboard must send live location after online availability succeeds.");
expect(dashboard.includes("KariGO will not update your live location"), "Dashboard must state location tracking stops when offline.");
expect(dashboard.includes("Today") && dashboard.includes("assigned deliveries"), "Dashboard must show today's assigned deliveries in compact stats.");
expect(dashboard.includes("Completed") && dashboard.includes("deliveries"), "Dashboard must show completed delivery summary in compact stats.");
expect(dashboard.includes("Active delivery"), "Dashboard must show active delivery, if any.");
expect(dashboard.includes("Assigned deliveries"), "Dashboard must show assigned deliveries.");
expect(dashboard.includes("Open active delivery"), "Dashboard must link to active delivery with delivery wording.");
expect(!dashboard.includes("Support and help"), "Dashboard must not repeat support blocks already available elsewhere.");
expect(!dashboard.includes("Operational guardrails"), "Dashboard must not show internal guardrail blocks on Home.");
expect(!dashboard.includes("Payout automation, withdrawals and ride dispatch stay disabled"), "Dashboard must remove internal readiness copy from Home.");
expect(!dashboard.includes("Apply for Ride review"), "Dashboard must keep Ride review entry points out of Home.");
expect(!dashboard.includes("Captain tools"), "Dashboard must not repeat the old Captain tools block.");
expect(riderNav.includes("Deliveries"), "Captain bottom nav must use delivery-focused copy.");
expect(jobsIndex.includes("Assigned Jobs"), "Jobs screen title must stay clear for dispatch assignments.");
expect(jobsIndex.includes("No delivery jobs assigned yet. Check again after dispatch assigns a delivery."), "Jobs list must have a captain-friendly empty state.");
expect(earnings.includes("Wallet withdrawals and payout requests require KariGO operations approval before activation."), "Earnings screen must state withdrawal guardrails.");
expect(profile.includes("Captain Profile"), "Profile screen must use polished Captain Profile title.");
expect(profile.includes("Captain tools"), "Profile must use Captain tool branding.");
expect(profile.includes("photoUrl") && profile.includes("Profile photo URL optional"), "Profile must support a safe profile photo URL update.");
expect(profile.includes("Completed deliveries") && profile.includes("Captain modes"), "Profile must show delivery stats and Captain modes.");
expect(profile.includes("preferredServiceAreas"), "Profile must save preferred Ride service areas for future dispatch readiness.");
expect(profile.includes("Ride service areas"), "Profile must keep preferred Ride service areas in Profile, not Home.");
expect(profile.includes("Use device GPS now"), "Profile must expose a GPS location update action.");
expect(profile.includes("disabled={profile.availabilityStatus === \"OFFLINE\"}"), "Profile must block live location updates while offline.");
expect(profile.includes("Go online before updating live location."), "Profile must show a clear offline location update message.");
expect(profile.includes("setBiometricSignIn") && profile.includes("Privacy Policy") && profile.includes("Terms"), "Profile must include biometric controls and in-app legal links.");
expect(profile.includes("Activity feed and notifications"), "Profile must link to notifications.");
expect(jobDetail.includes("Accept job") && jobDetail.includes("Reject job"), "Job detail must support accept/reject actions.");
expect(jobDetail.includes("RIDER_ARRIVING_PICKUP") && jobDetail.includes("PICKED_UP"), "Job detail must support pickup status progression.");
expect(jobDetail.includes("ON_THE_WAY") && jobDetail.includes("ARRIVED_DESTINATION"), "Job detail must support delivery status progression.");
expect(jobDetail.includes("Customer handoff"), "Job detail must guide the captain before delivery handoff.");
expect(jobDetail.includes("job.orderStatus === \"DELIVERED\""), "OTP completion must only appear after delivery is marked delivered.");
expect(!jobDetail.includes('job.orderStatus === "DELIVERED" || job.orderStatus === "ARRIVED_DESTINATION"'), "OTP completion must not appear at arrived-destination status.");
expect(jobDetail.includes("value.replace(/\\D/g, \"\").slice(0, 6)"), "Delivery OTP entry must accept only six digits.");
expect(jobDetail.includes("Complete delivery") && jobDetail.includes("Delivery completed successfully."), "Job detail must support OTP completion.");
expect(!jobsIndex.includes("deliveryOtp") && !jobDetail.includes("ordersApi.deliveryOtp"), "Rider app must not retrieve or display the customer delivery OTP.");
expect(taxiReadiness.includes("KariGO Rides operations"), "Ride operations screen must present live-ready operations copy when enabled.");
expect(taxiReadiness.includes("Ride operations will be available after KariGO approves your Captain account."), "Ride operations screen must block unapproved Captains with approved copy.");
expect(taxiReadiness.includes("guided Captain application") && taxiReadiness.includes("preferred operating areas"), "Ride readiness must send applicants to the guided Kano/Abuja-ready application.");
expect(taxiReadiness.includes("Ride operations"), "Ride operations copy must be used.");
expect(taxiReadiness.includes("Open guided Captain application"), "Ride readiness must route applicants to the secure guided Captain application flow.");
expect(taxiReadiness.includes("/auth/apply"), "Ride readiness must link to the guided Captain application route.");
expect(!taxiReadiness.includes("Driving licence document HTTPS link") && !taxiReadiness.includes("Vehicle particulars HTTPS link"), "Ride readiness must not expose stale document URL fields.");
expect(!taxiReadiness.includes("taxiApi.submitDriverApplication"), "Ride readiness screen must not keep the legacy free-text Ride application submitter.");
expect(taxiReadiness.includes("taxiApi.applicationStatus"), "Ride readiness screen must check application status where available.");
expect(taxiReadiness.includes("isTaxiStagingEnabled"), "Ride operations mode must use the shared controlled-pilot flag helper.");
expect(taxiReadiness.includes("fare payment and payout automation remain disabled"), "Ride operations mode must show payment and payout safety copy.");
expect(taxiReadiness.includes("taxiApi.updateAvailability"), "Ride review mode must support controlled availability updates.");
expect(taxiReadiness.includes("taxiApi.availableTrips"), "Ride review mode must fetch controlled available trips.");
expect(taxiReadiness.includes("Assigned ride trips"), "Ride operations must show assigned trips only.");
expect(taxiReadiness.includes("KariGO Operations will assign Ride requests when available."), "Ride operations empty state must explain Operations assignment.");
expect(taxiReadiness.includes("Accept assigned ride"), "Ride operations must accept manually assigned rides only.");
expect(taxiReadiness.includes("Start trip with PIN"), "Ride review mode must require customer PIN before trip start.");
expect(taxiReadiness.includes("Complete trip"), "Ride review mode must support completion after destination arrival.");
expect(taxiApi.includes("taxi/driver-applications"), "Ride API client must use the existing public application endpoint.");
expect(taxiApi.includes("rider/taxi/profile"), "Ride API client must expose the existing staging profile endpoint.");
expect(taxiApi.includes("rider/taxi/trips/available"), "Ride API client must expose the existing staging available-trip endpoint.");
expect(taxiApi.includes("rider/taxi/trips/${tripId}/start"), "Ride API client must submit the staging customer PIN to start a trip.");
expect(!taxiReadiness.includes("Pay Now") && !taxiReadiness.includes("cashout"), "Ride review mode must not expose payment or cashout actions.");

const locationHelper = read("src/lib/location.ts");
expect(locationHelper.includes("requestForegroundPermissionsAsync"), "Captain location helper must request foreground permission.");
expect(locationHelper.includes("getCurrentPositionAsync"), "Captain location helper must capture current device position.");

if (failures.length) {
  console.error("Rider staging regression check failed:");
  for (const failure of failures) console.error(`- ${failure}`);
  process.exit(1);
}

console.log("Rider staging regression check passed.");
