import { api } from "./client"; export interface DashboardMetrics { totalUsers:number;totalCustomers:number;totalVendors:number;activeVendors:number;totalRiders:number;onlineRiders:number;totalOrders:number;ordersToday:number;activeOrders:number;completedOrders:number;cancelledOrders:number;failedOrders:number;pendingSupportTickets:number;openRefundRequests:number;grossMerchandiseValue:string|number;deliveryFeeTotal:string|number;pendingVendorSettlements:string|number;pendingRiderEarnings:string|number; } export const dashboardApi={get:()=>api.get<DashboardMetrics>("admin/dashboard")};
