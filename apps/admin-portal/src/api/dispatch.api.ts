import { api } from "./client"; export interface AvailableRider{id:string;riderCode:string;name:string;phoneNumber:string;vehicleType?:string;availabilityStatus:string;activeJobCount:number} export const dispatchApi={available:()=>api.get<AvailableRider[]>("admin/riders/available"),assign:(orderId:string,riderId:string)=>api.post(`admin/orders/${orderId}/assign-rider`,{riderId}),reassign:(orderId:string,riderId:string)=>api.post(`admin/orders/${orderId}/reassign-rider`,{riderId})};
