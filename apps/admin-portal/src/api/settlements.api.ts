import { api } from "./client"; export const settlementsApi={vendors:(status?:string)=>api.get<any[]>(`admin/settlements/vendors${status?`?status=${status}`:""}`),riders:(status?:string)=>api.get<any[]>(`admin/settlements/riders${status?`?status=${status}`:""}`),payVendor:(id:string)=>api.post(`admin/settlements/vendors/${id}/mark-paid`),payRider:(id:string)=>api.post(`admin/settlements/riders/${id}/mark-paid`)};
