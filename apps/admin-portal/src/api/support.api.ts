import { api } from "./client"; export const supportApi={list:(q="")=>api.get<any[]>(`admin/support/tickets${q?`?${q}`:""}`),detail:(id:string)=>api.get<any>(`admin/support/tickets/${id}`),assign:(id:string,adminUserId:string)=>api.post(`admin/support/tickets/${id}/assign`,{adminUserId}),status:(id:string,status:string,priority?:string)=>api.patch(`admin/support/tickets/${id}/status`,{status,priority}),message:(id:string,message:string,isInternalNote=false)=>api.post(`admin/support/tickets/${id}/messages`,{message,isInternalNote})};
