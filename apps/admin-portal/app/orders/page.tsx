"use client";import{useEffect,useState}from"react";import{ordersApi,AdminOrder}from"../../src/api/orders.api";import{PortalShell,Empty,ErrorMessage,Badge}from"../../src/components/portal";import{SimpleTable}from"../../src/components/simple-table";import{friendlyError,money}from"../../src/lib/errors";
const paymentLabel=(x:AdminOrder)=>x.paymentMethod==="CASH_ON_DELIVERY"?`Cash/POD (${x.cashCollectionStatus??"PENDING"})`:x.paymentMethod==="WALLET"?"Wallet":x.paymentMethod==="SQUAD"?"Squad":x.paymentStatus;
export default function Orders(){const[o,setO]=useState<AdminOrder[]>([]),[status,setStatus]=useState(""),[search,setSearch]=useState(""),[error,setError]=useState("");const load=()=>ordersApi.list(new URLSearchParams(Object.fromEntries(Object.entries({status,search}).filter(([,v])=>v))).toString()).then(setO).catch(e=>setError(friendlyError(e)));useEffect(()=>{void load()},[status]);return <PortalShell><h1>Orders</h1><div className="filters"><select value={status}onChange={e=>setStatus(e.target.value)}><option value="">All statuses</option><option>READY_FOR_PICKUP</option><option>RIDER_ASSIGNED</option><option>COMPLETED</option><option>REFUND_REQUESTED</option></select><input value={search}onChange={e=>setSearch(e.target.value)}placeholder="Order number"/><button onClick={load}>Search</button></div><ErrorMessage>{error}</ErrorMessage>{o.length?<SimpleTable headers={["Order","Customer","Vendor","Status","Payment","Total"]}rows={o.map(x=>[<a href={`/orders/${x.id}`}>{x.orderNumber}</a>,x.customer.user.fullName,x.vendor?.businessName??"-",<Badge>{x.orderStatus}</Badge>,paymentLabel(x),money(x.totalAmount)])}/>:<Empty>No orders found for the selected filters.</Empty>}</PortalShell>}
