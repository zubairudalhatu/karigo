const fs = require("node:fs");
const path = require("node:path");

const appRoot = path.resolve(__dirname, "..");
const read = (relativePath) => fs.readFileSync(path.join(appRoot, relativePath), "utf8");
const readJson = (relativePath) => JSON.parse(read(relativePath));

const failures = [];
const expect = (condition, message) => {
  if (!condition) failures.push(message);
};

const packageJson = readJson("package.json");
const appJson = readJson("app.json");
const easJson = readJson("eas.json");
const appConfig = read("app.config.ts");
const rootLayout = read("app/_layout.tsx");
const login = read("app/auth/login.tsx");
const forgotPassword = read("app/auth/forgot-password.tsx");
const resetPassword = read("app/auth/reset-password.tsx");
const dashboard = read("app/index.tsx");
const registerIndex = read("app/register/index.tsx");
const registerVerify = read("app/register/verify.tsx");
const registerPassword = read("app/register/password.tsx");
const registerAccountType = read("app/register/account-type.tsx");
const registerBusiness = read("app/register/business.tsx");
const registerDocuments = read("app/register/documents.tsx");
const registerReview = read("app/register/review.tsx");
const registerSuccess = read("app/register/success.tsx");
const orders = read("app/orders/index.tsx");
const orderDetail = read("app/orders/[orderId].tsx");
const products = read("app/products/index.tsx");
const productNew = read("app/products/new.tsx");
const productEdit = read("app/products/[productId].tsx");
const services = read("app/services/index.tsx");
const serviceNew = read("app/services/new.tsx");
const serviceEdit = read("app/services/[serviceId].tsx");
const earnings = read("app/earnings/index.tsx");
const payout = read("app/payout/index.tsx");
const documents = read("app/documents/index.tsx");
const profile = read("app/profile/index.tsx");
const profileEdit = read("app/profile/edit.tsx");
const apiClient = read("src/api/client.ts");
const authContext = read("src/contexts/auth-context.tsx");
const partnerApi = read("src/api/partner.api.ts");
const authApi = read("src/api/auth.api.ts");
const registrationApi = read("src/api/registration.api.ts");
const uploadsApi = read("src/api/uploads.api.ts");
const nav = read("src/components/partner-navigation.tsx");
const labels = read("src/lib/labels.ts");
const partnerProfile = read("src/lib/partner-profile.ts");
const productForm = read("src/components/product-form.tsx");
const serviceForm = read("src/components/service-form.tsx");
const uploadPickers = read("src/lib/upload-pickers.ts");
const registrationContext = read("src/contexts/partner-registration-context.tsx");

expect(packageJson.name === "@karigo/partner-app", "Partner app package name must be @karigo/partner-app.");
expect(packageJson.main === "expo-router/entry", "Partner app must use Expo Router entry.");
expect(packageJson.dependencies?.["expo"] === "~53.0.0", "Partner app must use Expo SDK 53.");
expect(packageJson.dependencies?.["@expo/vector-icons"] === "^14.1.0", "Partner app must declare vector icons for bottom navigation.");
expect(packageJson.dependencies?.["expo-secure-store"] === "~14.2.3", "Partner app must persist tokens with Expo SecureStore.");
expect(packageJson.dependencies?.["expo-image-picker"] === "~16.1.4", "Partner app must declare Expo Image Picker for mobile image uploads.");
expect(packageJson.dependencies?.["expo-document-picker"] === "~13.1.6", "Partner app must declare Expo Document Picker for mobile document uploads.");
expect(packageJson.dependencies?.["expo-build-properties"] === "~0.14.8", "Partner app must use Expo build-properties for Android API 36 readiness.");
expect(!JSON.stringify(packageJson).includes("eas-cli"), "Partner app must not depend on eas-cli.");

expect(appJson.expo.name === "KariGO Partner", "Base app.json name must be KariGO Partner.");
expect(appJson.expo.slug === "karigo-partner", "Base app.json slug must be karigo-partner.");
expect(appJson.expo.scheme === "karigo-partner", "Base app.json scheme must be karigo-partner.");
expect(appJson.expo.extra?.eas?.projectId === "44e595bd-739a-430f-8d4d-99e961ac2451", "Partner app must keep the linked EAS project ID.");
expect(appJson.expo.updates?.url === "https://u.expo.dev/44e595bd-739a-430f-8d4d-99e961ac2451", "Partner app must keep the linked EAS updates URL.");
expect(appJson.expo.android?.package === "com.karigo.partner", "Partner app base Android package must remain com.karigo.partner.");
expect(appJson.expo.android?.versionCode === 3, "Partner app base Android versionCode must be 3 for the next Play-ready AAB.");
expect(appJson.expo.ios?.bundleIdentifier === "com.karigo.partner", "Partner app base iOS bundle identifier must remain com.karigo.partner.");
expect(JSON.stringify(appJson.expo.plugins).includes("expo-image-picker"), "Base app.json must configure Expo Image Picker.");
expect(JSON.stringify(appJson.expo.plugins).includes("expo-document-picker"), "Base app.json must configure Expo Document Picker.");
expect(JSON.stringify(appJson.expo.plugins).includes("expo-build-properties"), "Base app.json must configure Expo build-properties.");
expect(JSON.stringify(appJson.expo.plugins).includes('"targetSdkVersion":36'), "Base app.json must target Android API 36.");

expect(appConfig.includes("KariGO Partner"), "App config must use KariGO Partner branding.");
expect(appConfig.includes("KariGO Partner Staging"), "App config must support staging branding.");
expect(appConfig.includes("com.karigo.partner"), "Production package must be com.karigo.partner.");
expect(appConfig.includes("com.karigo.partner.staging"), "Staging package must be com.karigo.partner.staging.");
expect(appConfig.includes("karigo-partner"), "Production scheme must be karigo-partner.");
expect(appConfig.includes("EXPO_PUBLIC_API_BASE_URL"), "App config must read EXPO_PUBLIC_API_BASE_URL.");
expect(appConfig.includes("expo-image-picker") && appConfig.includes("expo-document-picker"), "App config must register mobile upload plugins.");
expect(appConfig.includes("expo-build-properties"), "App config must register Android build-properties.");
expect(appConfig.includes("compileSdkVersion: 36"), "Partner app must compile against Android API 36.");
expect(appConfig.includes("targetSdkVersion: 36"), "Partner app must target Android API 36.");
expect(appConfig.includes('buildToolsVersion: "36.0.0"'), "Partner app must use Android build tools 36.0.0.");
expect(appConfig.includes("versionCode: isStaging ? 1 : 3"), "Partner production versionCode must be 3 for the next Play-ready AAB.");
expect(!appConfig.includes("https://u.expo.dev/"), "Partner app must not configure an Expo Updates URL before EAS project linking.");

expect(easJson.build?.["partner-staging"]?.distribution === "internal", "partner-staging must be internal.");
expect(easJson.build?.["partner-staging"]?.android?.buildType === "apk", "partner-staging must produce APK.");
expect(easJson.build?.["partner-production"]?.distribution === "store", "partner-production must be store distribution.");
expect(easJson.build?.["partner-production"]?.android?.buildType === "app-bundle", "partner-production must produce AAB.");
expect(
  easJson.build?.["partner-production"]?.env?.EXPO_PUBLIC_API_BASE_URL === "https://karigo-8htn.onrender.com/api/v1",
  "partner-production must point at the production Render API."
);

expect(apiClient.includes("karigo_partner_session_v2"), "Partner app must persist access/refresh tokens in the v2 session envelope.");
expect(apiClient.includes("LEGACY_REFRESH_TOKEN_KEY"), "Partner app must keep legacy refresh token migration support.");
expect(apiClient.includes("refreshAuth"), "Partner app must support refresh sessions.");
expect(apiClient.includes("refresh_joined_existing"), "Partner app must use a single-flight refresh guard.");
expect(apiClient.includes("validateMobileApiBaseUrl"), "Partner API client must validate production API base URL safety.");
expect(apiClient.includes("unauthorized_non_definitive_preserved"), "Partner API client must preserve saved sessions for non-definitive auth failures.");
expect(authContext.includes("canUsePartnerApp(currentUser)"), "Partner app must authorize compatible accounts after authentication.");
expect(authContext.includes('user.role === "VENDOR" || user.role === "CUSTOMER" || user.role === "RIDER"'), "Partner app must accept approved Partner, recognised Customer and Captain accounts.");
expect(authContext.includes("bootstrap_auth_status_preserved"), "Partner auth bootstrap must preserve saved sessions when the backend auth state is temporarily unavailable.");
expect(authContext.includes("cannot access Partner onboarding"), "Role rejection copy must use Partner onboarding branding.");
expect(login.includes("KariGO Partner"), "Login screen must use KariGO Partner branding.");
expect(login.includes("Start Partner Onboarding"), "Login screen must link new partners to onboarding.");
expect(login.includes('router.push("/register")'), "Login screen must route onboarding to the in-app Partner registration flow.");
expect(!login.includes("https://vendor.karigo.com.ng/register"), "Login screen must not send new partners to browser-only registration.");
expect(login.includes("Forgot password") && login.includes("/auth/forgot-password"), "Login screen must expose forgot password.");
expect(login.includes("passwordVisible"), "Login screen must include password visibility controls.");
expect(forgotPassword.includes("authApi.requestPasswordReset"), "Forgot password screen must request partner reset OTP.");
expect(forgotPassword.includes("authApi.requestVendorActivationLink"), "Forgot password screen must support approved-vendor activation link requests.");
expect(resetPassword.includes("authApi.confirmPasswordReset"), "Reset password screen must confirm OTP and new password.");
expect(resetPassword.includes("passwordVisible"), "Reset password screen must include password visibility controls.");

expect(rootLayout.includes("PartnerBottomNav"), "Root layout must mount Partner bottom navigation.");
expect(rootLayout.includes("PartnerRegistrationProvider"), "Root layout must provide in-app Partner registration state.");
expect(rootLayout.includes("auth/forgot-password") && rootLayout.includes("auth/reset-password"), "Root layout must register password recovery routes.");
expect(rootLayout.includes("register/index") && rootLayout.includes("register/success"), "Root layout must register Partner registration routes.");
expect(rootLayout.includes("orders/[orderId]"), "Root layout must register the order detail route.");
expect(rootLayout.includes("products/new") && rootLayout.includes("products/[productId]"), "Root layout must register product create/edit routes.");
expect(rootLayout.includes("services/new") && rootLayout.includes("services/[serviceId]"), "Root layout must register service create/edit routes.");
expect(rootLayout.includes("earnings/index") && rootLayout.includes("payout/index"), "Root layout must register earnings and payout routes.");
expect(rootLayout.includes("profile/edit"), "Root layout must register partner profile edit route.");
expect(nav.includes("Home") && nav.includes("Orders") && nav.includes("Products") && nav.includes("Services") && nav.includes("Profile"), "Bottom nav must expose Partner foundation tabs.");
expect(nav.includes("Earnings"), "Bottom nav must expose earnings and settlements.");
expect(nav.includes("@expo/vector-icons") && nav.includes("Feather"), "Bottom nav must use proper icons.");
expect(!nav.includes('icon: "H"') && !nav.includes('icon: "O"'), "Bottom nav must not use first-letter icon substitutes.");
expect(nav.includes("pathname.startsWith(\"/auth\")"), "Bottom nav must hide on auth screens.");
expect(nav.includes("pathname.startsWith(\"/register\")"), "Bottom nav must hide on registration screens.");
expect(nav.includes("partnerApi.onboardingState"), "Bottom nav must check Partner onboarding state before showing workspace tabs.");
expect(nav.includes('state.state === "approved"'), "Bottom nav must show only after the Partner profile is approved.");
expect(nav.includes("useSafeAreaInsets") && nav.includes("Math.max(insets.bottom, 12)"), "Bottom nav must respect bottom safe-area insets.");

expect(authApi.includes("auth/password-reset/request") && authApi.includes("auth/password-reset/confirm"), "Auth API must expose password reset endpoints.");
expect(authApi.includes("auth/vendor-onboarding/account") && authApi.includes("auth/vendor-onboarding/password"), "Auth API must expose vendor applicant onboarding endpoints.");
expect(authApi.includes("auth/vendor/activation-link/request"), "Auth API must expose vendor activation link request endpoint.");
expect(registrationApi.includes("vendor-applications") && registrationApi.includes("submitVendorApplication"), "Registration API must submit public Partner applications.");
expect(registrationApi.includes("vendor-applications/me/ensure") && registrationApi.includes("ensurePartnerOnboarding"), "Registration API must ensure authenticated Partner onboarding draft state.");
expect(registrationApi.includes("vendor-applications/me/draft") && registrationApi.includes("savePartnerDraft"), "Registration API must persist authenticated Partner onboarding drafts.");
expect(registrationApi.includes("vendor-applications/me/submit") && registrationApi.includes("submitCurrentUserVendorApplication"), "Registration API must submit authenticated Partner applications idempotently.");
expect(uploadsApi.includes("vendors/uploads") && uploadsApi.includes("FormData") && uploadsApi.includes("Bearer"), "Upload API must use authenticated vendor-scoped multipart uploads.");
expect(uploadPickers.includes("expo-image-picker") && uploadPickers.includes("expo-document-picker"), "Upload picker helper must use native image and document pickers.");
expect(uploadPickers.includes("pickAndUploadImage") && uploadPickers.includes("pickAndUploadDocument"), "Upload picker helper must expose image and document upload helpers.");
expect(registrationContext.includes("PRODUCT_SELLER") && registrationContext.includes("SERVICE_PROVIDER") && registrationContext.includes("BOTH"), "Registration context must support all Partner account types.");
expect(registerIndex.includes("registrationApi.ensurePartnerOnboarding"), "In-app registration must resume authenticated Partner onboarding without requiring a duplicate applicant account.");
expect(registerIndex.includes("authApi.createVendorApplicantAccount") && registerIndex.includes("/register/verify"), "In-app registration must create a vendor applicant account and continue to OTP for new unauthenticated users.");
expect(registerIndex.includes("visibleFullName") && registerIndex.includes("visiblePhoneNumber") && registerIndex.includes("normalizedPhone"), "Registration start must validate the visible prefilled account values.");
expect(registerIndex.includes("formErrors") && registerIndex.includes("disabled={!canContinue}"), "Registration start must disable Continue only from visible validation errors.");
expect(registerIndex.includes("Your KariGO account has been recognised"), "Registration start must explain existing Customer account continuation.");
expect(registerVerify.includes("authApi.verifyVendorApplicantOtp") && registerVerify.includes("authApi.resendVendorApplicantOtp"), "In-app registration must verify and resend OTP.");
expect(registerPassword.includes("authApi.createVendorApplicantPassword"), "In-app registration must create applicant password after OTP.");
expect(registerAccountType.includes("Product Seller") && registerAccountType.includes("Service Provider") && registerAccountType.includes("Both"), "In-app registration must collect Partner account type.");
expect(registerBusiness.includes("Kano") && registerBusiness.includes("Abuja") && registerBusiness.includes("SME_SERVICES"), "In-app registration business step must support launch cities and service providers.");
expect(registerDocuments.includes("businessRegistrationDocumentReady") && registerDocuments.includes("identityDocumentReady"), "In-app registration must collect document readiness placeholders.");
expect(registerReview.includes("registrationApi.submitCurrentUserVendorApplication") && registerReview.includes("registrationApi.submitVendorApplication"), "In-app registration review must submit through authenticated onboarding when possible and preserve public fallback.");
expect(registerReview.includes("Complete your Business Address before submitting."), "In-app registration review must show clear missing Business Address copy.");
expect(registerReview.includes("partner-mobile-app"), "In-app registration review must submit application with mobile source metadata.");
expect(registerSuccess.includes("Application submitted") && registerSuccess.includes("Back to sign in"), "In-app registration must show a safe submission success screen.");
expect(partnerApi.includes("vendors/me"), "Partner API must load existing vendor/partner profile endpoint.");
expect(partnerApi.includes("vendors/capabilities") && partnerApi.includes("PartnerCapabilities"), "Partner API must load Partner capability projection.");
expect(partnerApi.includes("updateProfile") && partnerApi.includes('api.patch<PartnerProfile>("vendors/me"'), "Partner API must update profile/availability through the vendor profile endpoint.");
expect(partnerApi.includes("vendor-dashboard/orders"), "Partner API must load existing vendor orders endpoint.");
expect(partnerApi.includes("orderDetail") && partnerApi.includes("vendor-dashboard/orders/${orderId}"), "Partner API must expose order detail lookup.");
expect(partnerApi.includes("vendor/products"), "Partner API must load existing vendor products endpoint.");
expect(partnerApi.includes("createProduct") && partnerApi.includes('api.post<ProductSummary>("vendor/products"'), "Partner API must create products through vendor-owned endpoints.");
expect(partnerApi.includes("updateProduct") && partnerApi.includes("vendor/products/${productId}"), "Partner API must update products through vendor-owned endpoints.");
expect(partnerApi.includes("updateProductAvailability") && partnerApi.includes("vendor/products/${productId}/availability"), "Partner API must update product availability safely.");
expect(partnerApi.includes("vendors/services"), "Partner API must load existing vendor services endpoint.");
expect(partnerApi.includes("createService") && partnerApi.includes('api.post<VendorServiceSummary>("vendors/services"'), "Partner API must create vendor services through existing service endpoints.");
expect(partnerApi.includes("updateService") && partnerApi.includes("vendors/services/${serviceId}"), "Partner API must update vendor services through owned endpoints.");
expect(partnerApi.includes("archiveService") && partnerApi.includes('api.delete<VendorServiceSummary>(`vendors/services/${serviceId}`'), "Partner API must archive vendor services safely.");
expect(partnerApi.includes("vendors/onboarding-documents"), "Partner API must load onboarding document endpoint.");
expect(partnerApi.includes("uploadOnboardingDocument") && partnerApi.includes('api.post<PartnerOnboardingDocument>("vendors/onboarding-documents"'), "Partner API must submit onboarding document metadata.");
expect(partnerApi.includes("vendor/settlements"), "Partner API must load vendor settlement visibility endpoint.");
expect(partnerApi.includes("vendor/payout-account"), "Partner API must maintain vendor payout account details through vendor-scoped endpoints.");
expect(!partnerApi.includes("admin/vendor-payout-accounts"), "Partner app must not call admin payout account endpoints.");

expect(dashboard.includes("Product Seller") && dashboard.includes("Service Provider") && dashboard.includes("Both"), "Dashboard must describe supported partner types.");
expect(dashboard.includes("Your KariGO account has been recognised."), "Dashboard must handle recognised Customer accounts without a Partner profile safely.");
expect(dashboard.includes("Your Partner application is under review."), "Dashboard must show submitted Partner application state.");
expect(dashboard.includes("Partner access needs support."), "Dashboard must show restricted Partner access state safely.");
expect(dashboard.includes("Partner application was not approved."), "Dashboard must show rejected Partner application state safely.");
expect(dashboard.includes("Contact Support"), "Missing profile state must expose support.");
expect(dashboard.includes('router.push("/register")'), "Missing profile state must route to in-app Partner onboarding.");
expect(dashboard.includes("No active orders yet"), "Dashboard must have a safe empty order state.");
expect(dashboard.includes("Open order detail"), "Dashboard latest active order must open order detail.");
expect(dashboard.includes("partnerProfileWarning"), "Dashboard must warn for closed/demo partner profiles.");
expect(dashboard.includes("Go Online") && dashboard.includes("Go Offline"), "Dashboard must support partner online/offline availability.");
expect(dashboard.includes("partnerApi.updateProfile({ isOpen"), "Dashboard availability must use the safe profile update endpoint.");
expect(dashboard.includes("Add product") && dashboard.includes("View earnings and settlements") && dashboard.includes("Edit partner profile"), "Dashboard must expose operations shortcuts.");
expect(orders.includes("Partner orders"), "Orders screen must be Partner branded.");
expect(orders.includes("router.push(`/orders/${order.id}`)"), "Orders list cards must navigate to order detail.");
expect(orders.includes("Tap to view order detail"), "Orders list must show a clear tap affordance.");
expect(orderDetail.includes("Order detail"), "Order detail route must render order detail heading.");
expect(orderDetail.includes("partnerApi.orderDetail"), "Order detail route must fetch order detail.");
expect(orderDetail.includes("Status history"), "Order detail route must show status history.");
expect(orderDetail.includes("Read-only order view"), "Order detail route must stay read-only.");
expect(products.includes("Product Seller"), "Products screen must use Product Seller language.");
expect(products.includes("partnerApi.capabilities") && products.includes("canManageProducts"), "Products screen must hide product management for service-only partners.");
expect(products.includes("Products unavailable for this account"), "Products screen must show a clear capability unavailable state.");
expect(products.includes("Add product") && products.includes("Mark unavailable") && products.includes("Mark available"), "Products screen must support add and availability actions.");
expect(productNew.includes("partnerApi.createProduct") && productNew.includes("Create product"), "Product create route must call partnerApi.createProduct.");
expect(productEdit.includes("partnerApi.product") && productEdit.includes("partnerApi.updateProduct"), "Product edit route must load and update vendor-owned products.");
expect(productForm.includes("Product image URL must start with https://"), "Product form must enforce HTTPS product image URLs.");
expect(productForm.includes('pickAndUploadImage("product-image")') && productForm.includes("Upload product image"), "Product form must support controlled mobile product image upload.");
expect(services.includes("Service Provider"), "Services screen must use Service Provider language.");
expect(services.includes("partnerApi.capabilities") && services.includes("canManageServices"), "Services screen must hide service management for product-only partners.");
expect(services.includes("Add service") && services.includes("Mark unavailable") && services.includes("Mark available"), "Services screen must support service create and availability actions.");
expect(services.includes("partnerApi.archiveService") && services.includes("Archive"), "Services screen must support safe service archive.");
expect(serviceNew.includes("partnerApi.createService") && serviceNew.includes("Create service"), "Service create route must call partnerApi.createService.");
expect(serviceEdit.includes("partnerApi.service") && serviceEdit.includes("partnerApi.updateService") && serviceEdit.includes("partnerApi.archiveService"), "Service edit route must load, update and archive vendor-owned services.");
expect(serviceForm.includes("serviceProviderTypes") && serviceForm.includes("VendorServiceInput"), "Service form must use shared service provider types and input contract.");
expect(serviceForm.includes("Service image URL must start with https://"), "Service form must enforce HTTPS service image URLs.");
expect(serviceForm.includes('pickAndUploadImage("service-image")') && serviceForm.includes("Upload service image"), "Service form must support controlled mobile service image upload.");
expect(serviceForm.includes("HEALTH_PROFESSIONAL") && serviceForm.includes("Readiness only"), "Service form must keep health professional services readiness-gated.");
expect(earnings.includes("Automated payouts remain disabled") && earnings.includes("partnerApi.settlements"), "Earnings screen must show settlement visibility without automated payouts.");
expect(earnings.includes("Manage payout account"), "Earnings screen must link payout account setup.");
expect(payout.includes("partnerApi.payoutAccount") && payout.includes("partnerApi.createPayoutAccount") && payout.includes("partnerApi.updatePayoutAccount"), "Payout screen must support vendor payout account create/update.");
expect(payout.includes("This does not trigger payouts") && payout.includes("No money is sent from this screen"), "Payout screen must make payout automation guardrail explicit.");
expect(documents.includes("pickAndUploadDocument") && documents.includes("partnerApi.uploadOnboardingDocument"), "Documents screen must support controlled mobile onboarding document upload.");
expect(profile.includes("Edit partner profile") && profile.includes("Payout account") && profile.includes("View earnings"), "Profile must expose operations and payout routes.");
expect(profile.includes("partnerProfileWarning"), "Profile must warn for closed/demo partner profiles.");
expect(profile.includes("partnerApi.onboardingState"), "Profile must resolve application state before showing profile actions.");
expect(profile.includes("Partner profile not active yet"), "Profile must not show contradictory approved-profile actions before approval.");
expect(profile.includes("Log out"), "Profile screen must expose logout.");
expect(profileEdit.includes("partnerApi.updateProfile") && profileEdit.includes("Save profile"), "Profile edit route must save through vendor profile endpoint.");
expect(profileEdit.includes("Partner profile editing is not active yet"), "Profile edit must block editing before Partner profile approval.");
expect(profileEdit.includes("Logo URL") && profileEdit.includes("Cover image URL"), "Profile edit must keep HTTPS branding URL fields alongside mobile uploads.");
expect(profileEdit.includes('pickAndUploadImage(kind)') && profileEdit.includes("Upload logo") && profileEdit.includes("Upload cover image"), "Profile edit must support controlled mobile logo and cover uploads.");
expect(labels.includes("Pay on Delivery") && labels.includes("formatLabel") && labels.includes("statusTone"), "Partner app must format raw enum labels safely.");
expect(labels.includes("Pending verification") && labels.includes("Verified"), "Partner app must format payout account statuses safely.");
expect(partnerProfile.includes("Partner record requires confirmation") && partnerProfile.includes("Partner profile is closed or inactive"), "Partner profile helper must handle flagged and closed profiles.");

if (failures.length) {
  console.error("Partner app regression check failed:");
  for (const failure of failures) console.error(`- ${failure}`);
  process.exit(1);
}

console.log("Partner app regression check passed.");
